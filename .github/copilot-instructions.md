# Copilot Instructions for max-shajason

## Project Overview
This repository appears to be a starter or template project, possibly for educational or assignment purposes, with minimal existing content. The only file present is a README with a MakeCode badge, suggesting integration with Microsoft MakeCode or a similar online IDE.

## Key Guidelines for AI Agents
- **Project Purpose:** This repo is likely intended for hands-on coding assignments, such as implementing a game or interactive project (e.g., Snake game).
- **Entry Point:** No code files exist yet. You should create a main source file (e.g., `snake.py`, `main.py`, or `snake.js`) in the root directory unless otherwise specified by the user.
- **Platform:** If targeting MakeCode, use JavaScript/TypeScript or Python, following MakeCode's conventions. Otherwise, default to Python for general educational projects unless the user requests another language.
- **No Build System:** There are no build scripts or configuration files. Assume a single-file or simple multi-file structure.
- **No Tests or Linting:** No test or linting setup is present. If adding tests, place them in a `tests/` directory and use simple, standard frameworks (e.g., `unittest` for Python).
- **Documentation:** Update the README with instructions for running or interacting with the project after adding new features.
- **External Dependencies:** Avoid adding dependencies unless explicitly requested. Prioritize standard libraries and browser APIs (for JS/TS) or Python built-ins.
- **File/Folder Structure:**
  - Place main code files in the root directory.
  - Use `.github/` for workflow or agent instructions only.
  - Add a `docs/` folder for additional documentation if needed.

## Example: Adding a Snake Game
- Create `snake.py` (Python) or `snake.js` (JavaScript) in the root.
- Use simple, self-contained logic for game loop, rendering, and input.
- If using MakeCode, follow its API for drawing and input handling.

## When in Doubt
- Ask the user for clarification if the target language, platform, or requirements are unclear.
- Keep code and instructions simple and beginner-friendly.

---
_Last updated: 2026-02-09_
